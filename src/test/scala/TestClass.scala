import org.scalatest._

object TestClass extends FlatSpec{

  "The first query" should "result in size * " in {
// call the query and assert the test
  }

  it should "produce first rows : **" in {
// call querues.animCount
  }
}
